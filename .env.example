# ===========================================
# OpenTable Monitor Configuration
# ===========================================
# Copy this to .env and customize as needed
# For Railway: Set these as environment variables in Railway dashboard

# Server Configuration
PORT=3001
NODE_ENV=production
LOG_LEVEL=info

# Performance Settings
MAX_CONCURRENCY=6          # Max parallel browser tabs (1-10)
MAX_DAYS_TO_CHECK=90       # Max days allowed to check

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000   # 15 minutes in milliseconds
RATE_LIMIT_MAX=100            # Max requests per window

# CORS (comma-separated list of allowed origins for production)
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# ===========================================
# Notifications (for Continuous Monitoring)
# ===========================================
# When continuous monitoring finds availability,
# notifications will be sent via these channels

# Slack Notifications (RECOMMENDED - Easiest setup!)
# 1. Go to https://api.slack.com/apps
# 2. Create new app -> From scratch
# 3. Add "Incoming Webhooks" feature
# 4. Create webhook for your channel
# 5. Copy the webhook URL below
SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Email Notifications
# For Gmail, use an App Password: https://support.google.com/accounts/answer/185833
NOTIFY_EMAIL=your@email.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=OpenTable Monitor <your@gmail.com>

# ===========================================
# Railway Deployment
# ===========================================
# Railway sets PORT automatically
# Set these in Railway Dashboard -> Variables:
# - SLACK_WEBHOOK (for notifications)
# - Any other config you want to customize
